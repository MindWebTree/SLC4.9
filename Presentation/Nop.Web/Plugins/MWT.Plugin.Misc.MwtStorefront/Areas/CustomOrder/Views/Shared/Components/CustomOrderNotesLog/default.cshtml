@model List<CustomOrderNotesLogModel>

@if (Model.Count > 0)
{
    foreach (var log in Model)
    {
        @Html.Raw(log.Notes + " - Updated by " + log.CustomerName + " on " + log.CreatedOn)<br />
    }
}