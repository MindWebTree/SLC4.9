@model CustomCatalogProductsModel
@inject ICustomWorkContext _workContext;
@if (Model.Products.Count <= 0 &&
!Model.PriceRangeFilter.Enabled &&
!Model.SpecificationFilter.Enabled &&
!Model.ManufacturerFilter.Enabled)
{
    return;
}
@{
    var hidefilter = Model.SpecificationFilter.Attributes.Count==0;
    var displayFilters = false;
    if (!_workContext.IsMobileDevice())
    {
    

        if (Model.SpecificationFilter.Attributes.Where(m => m.Values.Where(m => m.Selected).Count() > 0).Any())
            displayFilters = true;
    }
    string featuredString = Context.Request.Query["featuredid"];

    int productId = 0;
    if (!string.IsNullOrEmpty(featuredString))
    {
        string[] parts = featuredString.Split('-');

        int.TryParse(parts[0], out productId);
    }
}




<div class="filter-section">
    <div class="filter d-flex align-items-center">
        @*region Filter hamBurger*@
        @if (_workContext.IsMobileDevice())
        {
            <div tabindex="0" role="button" class="position-relative filterIcon  @(hidefilter ? "hide" : "")">

                <span class="text-filter" data-filter-text="@T("catalog.filter.text")" data-hidefilter-text="@T("catalog.hidefilter.text")">@T("catalog.filter.text") </span>

                <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" alt="@T(displayFilters ? "catalog.hidefilter.text" : "catalog.filter.text")">
                    <path d="M2.66667 13.9999V9.33325" stroke="#444444" stroke-width="1.33" stroke-linecap="round" stroke-linejoin="round" />
                    <path d="M2.66667 6.66667V2" stroke="#444444" stroke-width="1.33" stroke-linecap="round" stroke-linejoin="round" />
                    <path d="M8 14V8" stroke="#444444" stroke-width="1.33" stroke-linecap="round" stroke-linejoin="round" />
                    <path d="M8 5.33333V2" stroke="#444444" stroke-width="1.33" stroke-linecap="round" stroke-linejoin="round" />
                    <path d="M13.3333 14.0001V10.6667" stroke="#444444" stroke-width="1.33" stroke-linecap="round" stroke-linejoin="round" />
                    <path d="M13.3333 8V2" stroke="#444444" stroke-width="1.33" stroke-linecap="round" stroke-linejoin="round" />
                    <path d="M0.666672 9.33325H4.66667" stroke="#444444" stroke-width="1.33" stroke-linecap="round" stroke-linejoin="round" />
                    <path d="M6 5.33325H10" stroke="#444444" stroke-width="1.33" stroke-linecap="round" stroke-linejoin="round" />
                    <path d="M11.3333 10.6667H15.3333" stroke="#444444" stroke-width="1.33" stroke-linecap="round" stroke-linejoin="round" />
                </svg>

            </div>
        }
        else
        {
            <div tabindex="0" role="button" aria-label="Filters for @Model.CategoryName"
            class="position-relative filterDesktopIcon @(hidefilter ? "border-0" : "") @(displayFilters ? "show" : "")">
                <span class="text-filter @(hidefilter?"d-none":"")" data-filter-text="@T("catalog.filter.text")"
                      data-hidefilter-text="@T("catalog.hidefilter.text")">
                    @T(displayFilters ? "catalog.hidefilter.text" : "catalog.filter.text")
                </span>
                @if (!hidefilter)
                {
                <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" alt="@T(displayFilters ? "catalog.hidefilter.text" : "catalog.filter.text")">

                    <path d="M14 11.9999H8" stroke="#374151" stroke-width="1.77333" stroke-linecap="round" stroke-linejoin="round" />

                    <path d="M5.33333 11.9999H2" stroke="#374151" stroke-width="1.77333" stroke-linecap="round" stroke-linejoin="round" />

                    <path d="M14.0003 3.55713H10.667" stroke="#374151" stroke-width="1.77333" stroke-linecap="round" stroke-linejoin="round" />

                    <path d="M8 3.55725L2 3.55725" stroke="#374151" stroke-width="1.77333" stroke-linecap="round" stroke-linejoin="round" />

                    <path d="M5.33301 14.6665V9.33317" stroke="#374151" stroke-width="1.77333" stroke-linecap="round" stroke-linejoin="round" />

                    <path d="M10.667 6.22388V0.890544" stroke="#374151" stroke-width="1.77333" stroke-linecap="round" stroke-linejoin="round" />

                </svg>
                }
            </div>
        }

        @*Filter Icon end*@



       
            @await Component.InvokeAsync("QuickFilter", new { entityId = Model.CategoryID, entityType = "Category", heading = "" })
       
        @*sorting*@

        <div class="sort-by  d-sm-block ">
            <div id="noofprdDisplay" tabindex="0">
                @Html.Raw(string.Format(T("Label.Showing").Text, (Model.PageIndex + 1) * Model.PageSize > Model.TotalItems ? Model.TotalItems : (Model.PageIndex + 1) * Model.PageSize, Model.TotalItems))
            </div>
            @if (Model.AllowProductSorting)
            {
                <div class="sortbysection">
                    <div class="sortbylabel">
                        @T("Catalog.OrderBy")
                    </div>
                    <div class="custom-select">
                        <select aria-label="@T("Catalog.OrderBy.Label")" class="dropdown-filter" id="products-orderby" name="products-orderby">
                            @foreach (var sortOption in Model.AvailableSortOptions)
                            {
                                <option selected="@sortOption.Selected" value="@( sortOption.Text.Replace(" ","_"))">@sortOption.Text</option>
                            }

                        </select>

                    </div>
                </div>
                <script asp-location="Footer">
                       defaultSortOption = '@(Model.AvailableSortOptions.FirstOrDefault()?.Text ?? string.Empty)';
            $(document).ready(function () {
                var $orderByEl = $('#products-orderby');
                $orderByEl.on('change', function () {
                    CatalogProducts.getProducts();
                });

                $(CatalogProducts).on('before', function (e) {
                     if ($orderByEl.val() != defaultSortOption)
                        e.payload.urlBuilder
                            .addParameter('@T("SortOption.Query.Param")', "[" + $orderByEl.val() + "]");
                });
            });
                </script>
            }
        </div>

        @*page size*@
        @if (Model.AllowCustomersToSelectPageSize)
        {
            @*<div class="product-page-size col-12 col-sm-12 col-md-4 col-lg-4 col-xl-4 text-center d-inline-block">
                <div class="row">
                <div class="col-12 col-sm-12 col-md-4 col-lg-4 col-xl-4 product-span">
                <span> @T("Catalog.PageSize") </span>
                </div>
                <div class="col-12 col-sm-12 col-md-4 col-lg-4 col-xl-4">
                @Html.DropDownList("products-pagesize", Model.PageSizeOptions, new { @class = "form-control dropdown-filter", aria_label = T("Catalog.PageSize.Label") })
                </div>
                <div class="col-12 col-sm-12 col-md-4 col-lg-4 col-xl-4 product-span">
                <span> @T("Catalog.PageSize.PerPage") </span>
                </div>
                </div>
                </div>
                <script asp-location="Footer">
                $(document).ready(function () {
                var $pageSizeEl = $('#products-pagesize');
                $pageSizeEl.on('change', function () {
                CatalogProducts.getProducts();
                });

                $(CatalogProducts).on('before', function (e) {
                e.payload.urlBuilder
                .addParameter('pagesize', $pageSizeEl.val());
                });
                });
                </script>*@
        }
        @*view mode*@
        @if (Model.AllowProductViewModeChanging)
        {
            <div class="product-viewmode col-12 col-sm-12 col-md-4 col-lg-4 col-xl-4 text-center d-inline-block">
                <div class="row">
                    <div class="col-12 col-sm-12 col-md-4 col-lg-4 col-xl-4 product-span">
                        <span>@T("Catalog.ViewMode")</span>
                    </div>
                    @if (Model.AvailableViewModes.Count > 1)
                    {
                        var gridMode = Model.AvailableViewModes[0];
                        var listMode = Model.AvailableViewModes[1];
                        <div class="col-12 col-sm-12 col-md-8 col-lg-8 col-xl-8 grid-list-icon">
                            <a class="viewmode-icon grid @if(gridMode.Selected){
                <text>selected</text>
}" data-viewmode="@gridMode.Value" title="@gridMode.Text">@gridMode.Text</a>
<a class="viewmode-icon list @if (listMode.Selected){
                <text>selected</text>
}" data-viewmode="@listMode.Value" title="@listMode.Text">@listMode.Text</a>
</div>

}
                </div>
            </div>
            <script asp-location="Footer">
                $(document).ready(function () {
                    var $viewModeEls = $('[data-viewmode]');
                    $viewModeEls.on('click', function () {
                        if (!$(this).hasClass('selected')) {
                            $viewModeEls.toggleClass('selected');
                            CatalogProducts.getProducts();
                        }
                        return false;
                    });

                    $(CatalogProducts).on('before', function (e) {
                        var $viewModeEl = $('[data-viewmode].selected');
                        if ($viewModeEl) {
                            e.payload.urlBuilder
                                .addParameter('viewmode', $viewModeEl.data('viewmode'));
                        }
                    });
                });
            </script>
        }
    </div>
</div>

<script asp-location="Footer">

    jQuery(document).ready(function () {
        var windowWidth = jQuery(window).width(); // Get current window width
        if (windowWidth > 767) {
            var filterSection = jQuery('.category-listing-sort-filter-conatiner');
            if (filterSection.length) {
                var filterOffset = filterSection.offset().top; // Get initial position of the filter section

                jQuery(window).on("scroll", function () {
                    console.log('Scroll event fired');
                    var scrollPos = jQuery(window).scrollTop(); // Get current scroll position



                    if (scrollPos >= filterOffset) {
                        filterSection.addClass('fixed-filter');
                    } else {
                        filterSection.removeClass('fixed-filter');
                    }

                });
            } else {
                console.log('No filter section found');
            }
        }
    });

</script>