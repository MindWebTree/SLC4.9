@model CustomProductOverviewModel
@using Nop.Core
@using Nop.Core.Domain.Catalog
@using Nop.Core.Domain.Orders
@using Nop.Core.Domain.Tax
@inject CatalogSettings catalogSettings
@inject IWorkContext workContext

<div data-productid="@Model.Id">
    @*Tags*@
    <div class="Tags">
        @if (Model.Tags != null)
        {
            foreach (var tag in Model.Tags)
            {
                <div class="@tag.Name">@tag.Name</div>
                break;
            }
        }
    </div>
    <a href="@Url.RouteUrl("Product", new { id = Model.Id, SeName = Model.SeName })" aria-label="@Model.DefaultPictureModel.Title">
        <img alt="" src="/images/pixel.png" data-src="@Model.DefaultPictureModel.ImageUrl" class="lazy" />
    </a>

</div>



